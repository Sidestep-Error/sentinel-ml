apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sentinel
resources:
  - configmap.yaml
  # secret.yaml är gitignored — kopiera secret.example.yaml och fyll i värden
  # innan första apply. Se runbooks/sentinel-ml-deploy.md.
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
